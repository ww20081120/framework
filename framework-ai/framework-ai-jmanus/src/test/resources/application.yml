project: #项目信息
  name: framework-ai-jmanus
  version: 4.1
  model: dev

spring:
  application:
    name: ${project.name}
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}

#logging:
#  level:
#    root: DEBUG