project: #项目信息
 name: log-demo
  
spring: #应用配置
  application:
    name: ${project.name}   
 
server: #系统配置
  port: 10000
  
management:
  tracing:
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: http://localhost:16686/v1/traces